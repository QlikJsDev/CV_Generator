{
  "name": "cv-generator",
  "version": "1.0.0",
  "description": "CV generator — Beyond Data → Select Advisory",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "pizzip": "^3.1.4",
    "@xmldom/xmldom": "^0.8.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
