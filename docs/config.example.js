/**
 * CV Generator — local configuration template
 *
 * Copy this file to config.js (same folder) and fill in your keys.
 * config.js is listed in .gitignore and will never be committed.
 */
window.SA_CONFIG = {
  anthropicApiKey:  '',   // paste your sk-ant-… key here

  // Supabase (optional — enables profile save/load)
  // Create a project at https://supabase.com, then paste the values from:
  // Project Settings > API > Project URL & anon public key
  supabaseUrl:      '',   // https://xxxx.supabase.co
  supabaseAnonKey:  '',   // eyJ…
};
